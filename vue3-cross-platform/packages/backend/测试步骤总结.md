# 📋 测试步骤总结

## ✅ 已完成的工作

1. ✅ **后端服务已启动**（在后台运行）
2. ✅ **代码编译成功**（无错误）
3. ✅ **已创建详细的SQL更新脚本**

## ⚠️ 必须执行：更新数据库密码

### 为什么需要更新？

- 数据库中的密码是 **MD5格式**（旧）
- 代码使用 **BCrypt格式** 验证（新）
- 两种格式不匹配，导致登录失败

### 如何更新？（3步完成）

**步骤1**：打开MySQL客户端（命令行、Workbench、Navicat等）

**步骤2**：连接到MySQL数据库

**步骤3**：执行这3行SQL：

```sql
USE campus_db;

UPDATE `user` SET `password` = '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwy8pLKO' WHERE `username` IN ('zhangsan', 'lisi', 'wangwu', 'admin');
```

**完成！** 看到 "4 row(s) affected" 就表示成功了。

## 🚀 服务状态

- **服务地址**：http://localhost:8080
- **API文档**：http://localhost:8080/doc.html
- **状态**：正在启动中（可能需要30-60秒）

## 🧪 测试步骤

### 1. 等待服务启动完成

查看控制台，应该看到：
```
Started BackendApplication in X.XXX seconds
```

### 2. 更新数据库密码

执行上面的SQL（必须执行，否则登录会失败）

### 3. 测试登录

**方法A：使用浏览器**
1. 访问：http://localhost:8080/doc.html
2. 找到"用户管理" -> "用户登录"
3. 输入：`{"username":"zhangsan","password":"123456"}`
4. 点击发送

**方法B：使用curl**
```bash
curl -X POST http://localhost:8080/api/user/login -H "Content-Type: application/json" -d "{\"username\":\"zhangsan\",\"password\":\"123456\"}"
```

## 📝 测试账号

| 用户名 | 密码 |
|--------|------|
| zhangsan | 123456 |
| lisi | 123456 |
| wangwu | 123456 |
| admin | 123456 |

## 📁 已创建的文档

1. **UPDATE_PASSWORD_SIMPLE.sql** - 最简单的SQL（3行）
2. **UPDATE_PASSWORD_DETAILED.sql** - 带说明的SQL
3. **UPDATE_PASSWORD_STEP_BY_STEP.md** - 详细步骤说明
4. **立即执行-更新密码.md** - 快速指南
5. **完整测试指南.md** - 完整测试流程

## 🎯 下一步

1. **先执行SQL更新密码**（必须）
2. **等待服务完全启动**（30-60秒）
3. **测试登录接口**
4. **测试其他接口**

## ⚠️ 如果遇到问题

### 问题1：Knife4j文档显示"请求异常"
- **原因**：服务可能还在启动中
- **解决**：等待30-60秒后刷新页面

### 问题2：登录返回500错误
- **原因**：数据库密码未更新
- **解决**：执行SQL更新密码

### 问题3：无法连接数据库
- **原因**：MySQL服务未启动或配置错误
- **解决**：检查MySQL服务和配置

## 📞 需要帮助？

如果遇到问题，请：
1. 查看后端控制台日志
2. 检查数据库连接
3. 确认已执行SQL更新密码
